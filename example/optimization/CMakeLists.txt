set(PROGS bisection_1d golden_section_1d nelder_mead_2d newton_1d)
foreach(name ${PROGS})
  add_executable(${name} ${name}.cpp)
  target_link_libraries(${name} standards)
  add_test(${name} ${name})
endforeach(name)
